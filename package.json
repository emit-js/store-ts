{
  "name": "@emit-js/store",
  "version": "8.0.1",
  "description": "emit store",
  "keywords": [
    "immutable",
    "observable",
    "store"
  ],
  "author": "emit <emit@emit-js.com>",
  "main": "dist/store.js",
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/emit-js/store-ts.git"
  },
  "license": "MIT",
  "homepage": "https://github.com/emit-js/store-ts#readme",
  "scripts": {
    "fix": "npm run lint -- --fix",
    "lint": "eslint --ignore-path .gitignore --ext=js .",
    "pretty": "prettier --write **/*.css **/*.json **/*.md",
    "test": "jest",
    "build": "tsc",
    "docs": "typedoc --out docs --media media src"
  },
  "devDependencies": {
    "@emit-js/emit": "4.0.3",
    "@types/jest": "24.0.11",
    "@types/node": "11.13.4",
    "@typescript-eslint/eslint-plugin": "1.6.0",
    "eslint": "5.16.0",
    "eslint-config-prettier": "4.1.0",
    "eslint-plugin-prettier": "3.0.1",
    "husky": "1.3.1",
    "jest": "24.7.1",
    "lint-staged": "8.1.5",
    "prettier": "1.16.4",
    "ts-jest": "24.0.2",
    "typescript": "3.4.3"
  },
  "prettier": {
    "printWidth": 60,
    "semi": false,
    "trailingComma": "es5"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged && npm test"
    }
  },
  "lint-staged": {
    "*.{css,json,md}": [
      "prettier --write",
      "git add"
    ],
    "*.{js,jsx,ts}": [
      "eslint --ignore-path .gitignore --fix",
      "git add"
    ]
  }
}
